"""Agent seam adapters.

Codex: the ``codex app-server`` JSON-RPC surface, including its approval requests.
Claude: an MCP server for user-authored speech, the peer socket for
supervisor-authored pushes, and the ``PermissionRequest`` hook for approvals.
"""
